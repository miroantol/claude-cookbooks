# Hard-pinned so the deployment doesn't silently break on a future release.
# The npm @anthropic-ai/claude-code pin in the Dockerfile is part of the same
# pin set — bump together. The agent's own scripts and hooks use only the
# standard library, so nothing extra is needed for them.
claude-agent-sdk==0.1.50
fastapi==0.118.3
uvicorn[standard]==0.37.0
sse-starlette==2.1.3
python-dotenv==1.0.1
